# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/idn_actionmailer.rb'

Hoe.new('IdnActionmailer', IdnActionmailer::VERSION) do |p|
  # p.rubyforge_name = 'IdnActionmailerx' # if different than lowercase project name
  # p.developer('Mick Staugaard', 'mick (that funny a with te strange ring around it :) staugaard.com')
end

# vim: syntax=Ruby
